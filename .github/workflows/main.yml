name: Practice projecet

on:
  push:
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v4

      - name: install dependecies
        run: npm ci
      - name: lint
        run: npm run lint
      - name: test
        run: npm test
          

